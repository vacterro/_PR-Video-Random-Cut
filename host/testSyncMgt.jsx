try { var seq = app.project.activeSequence; var p = new File(v:\\___VAC\\__K\\__CODE\\_PLUG_SCRIPT\\_AutoPremiere\\assets\\subtitle.mogrt).fsName; var clip = seq.importMGT(p, seq.getPlayerPosition().seconds, 2, 2); var attempts = 0; while(!clip.getMGTComponent() && attempts < 20) { $.sleep(100); attempts++; } if(clip.getMGTComponent()) alert(Got it after  + attempts +  attempts); else alert(Still null); } catch(e) { alert(e); }
